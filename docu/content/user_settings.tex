%!TEX root = ../document/document.tex

% This file contains all user settings that are used in the document.


%%%%%%%%%% Language %%%%%%%%%%

% Currently the following languages are supported:
% - German (de)
% - English (en)
\setlang{de}


%%%%%%%%%% Infill Settings %%%%%%%%%%

% String settings
\settitlestr{Translator WebApp mit Terraform und Ansible}
\setpapertype{Cloud Computing 2 -- Abschlussprojekt}
\setdegree{Bachelor of Science}
\setstudycourse{IT-Automotive}
\setuni{Duale Hochschule Baden-Württemberg}
\setunilocation{Stuttgart}
\setauthorstr{Tim Baßmann}
\setstartdate{24.02.2025}
\setsubmissiondate{20.04.2025}
\setprocessingtime{\startdate\ - \submissiondate}
\setstudentid{4274403}
\setcourse{TINF22ITA}
\setcompany{Firma GmbH}
\setcompanylocation{Stuttgart}
\setsupervisor{Nils Riekers}
\setreviewer{Prof. Dr. rer. nat. Robert Reviewer}
\setheaderleft{\titlestr\\\studycourse}  % Is shown if one or both logos are disabled
\setdepartment{Abteilung}
\setsubmissionlocation{Stuttgart}
\setsigningdate{\today}                 % Adjust the date that appears on the blocking notice and the declaration -> \today for date of compilation
\setfootermiddle{CC2}

\setfontstr{lmodern}        % chicago-authordate, mla, apa; sonstige: ieee, numeric-comp, alphabetic, authoryear
\setfontsizestr{12pt}       % Size of the font
\setlinespace{1.5}          % Sets the line space. Choose between: 1, 1.5, 2

\setcitingstyle{ieee}       % ieee, apa, mla, chicago-authordate, alphabetic, numeric-comp, authoryear
\setchapterdistance{10pt}   % Distance between chapter title and the header


%%%%%%%%%% Applyable Settings %%%%%%%%%%

% \blockingnotetrue           % comment out if no blocking note is needed
% \declarationtrue            % comment out if no self-declaration is needed
% \abstractdetrue             % comment out if no German abstract is needed
% \abstractentrue             % comment out if no English abstract is needed
\tableofcontentstrue        % comment out if no table of contents is needed
\acronymstrue               % comment out if no list of acronyms is needed
\imagelisttrue              % comment out if no list of images is needed
% \tablelisttrue              % comment out if no list of tables is needed
% \symbolstrue                % comment out if no list of symbols is needed
% \formularytrue              % comment out if no list of formulas is needed
% \codelisttrue               % comment out if no list of code is needed
\sourcestrue                % comment out if no list of sources is needed
\appendixtrue               % comment out if no appendix is needed
% \glossarytrue               % comment out if no glossary is needed

% \showcompanylogotrue        % comment out if no company logo should be inserted
\showdhbwlogotrue           % comment out if no DHBW logo should be inserted
\showsignaturetrue          % comment out if no signature should be inserted

% \refwithpagestrue           % comment out if no page number should be added to references
% \reviewertrue	            % comment out if no reviewer is needed
% \showtodonotestrue          % comment out if no todos are needed

% Display a green, yellow or red dot on the cover sheet to indicate the privacy level
% \greendottrue               % comment out if no green dot is needed
% \yellowdottrue              % comment out if no yellow dot is needed
% \reddottrue                 % comment out if no red dot is needed
